" Josh's vimrc setup

" example file as a starting point
source $VIMRUNTIME/vimrc_example.vim

" Josh's stuff:
colorscheme josh-scheme
" disable mouse support since it interferes with term select/copy:
set mouse=
set modeline
filetype plugin on

